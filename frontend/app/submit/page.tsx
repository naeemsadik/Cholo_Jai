import { SubmitForm } from "./SubmitForm";

export const metadata = {
  title: "Submit Your Event · Cholo Jai",
  description: "Run something in Dhaka? Send it our way — manual review, free to list, goes live within 48 hours.",
};

export default function SubmitPage() {
  return <SubmitForm />;
}